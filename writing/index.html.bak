{% capture content %}

    <!-- ######################   Writing >> Header  ###################### -->

    <div class="grid  container--main  writing">

           <div class="grid__item  one-third  lap-one-whole  palm-one-whole">
                 <h2 class="writing__subheading"><a href="writing.html">Notes & Ruminations</a></h2>
           </div><!--

        --><div class="grid__item  two-thirds  lap-one-whole  palm-one-whole  about__intro">
                <h1 class="writing__heading">Writings, &c.</h1>

                <p class="writing__introtext">
                I hope the following space will serve as an area to test out various ideas and concepts.  As Mr. <a href="http://en.wikipedia.org/wiki/Michel_de_Montaigne" class="about__hyperlink" target=_blank>Montaigne</a> may have said,<em> “Qu'est-ce que je sais?”</em></p>
           </div>

            <hr class="rule--hero  rule--resume">
    </div>


    <!-- ######################   Writing Page >> Article Feed ###################### -->

     <!-- Begin Article Feed Content -->



<h1>Loop</h1>

{% for issue in taxonomy['Issue'] order:descending sort_by:title%}


  <div class="grid  container--main  writing__content">

        <div class="grid__item  one-third lap-one-whole  palm-one-whole  negative--top2x  issue">
                        <a href="{{issue.url}}"><span class="issue__number">{{issue}}<span><br></a>
              {% for topic in taxonomy['Topic'] %}
                {% for post in issue.posts %}
                  {% if post.taxonomy['Topic'].values contains topic.value %}
                    <p class="issue__overline">{{post.meta['issue__overline']}}</p>
                    <p class="issue__headline"> {{post.meta['issue__headline']}}</p>
                    <p class="issue__season">{{post.meta['issue__season']}}</p>
                  {% endif %}
                {% endfor %}
              {% endfor %}
          </div><!--

       --><div class="grid__item  two-thirds  lap-one-whole  palm-one-whole  negative--top2x  writing__feed">
              {% for topic in taxonomy['Topic']  %}
                {% for post in issue.posts %}
                  {% if post.taxonomy['Topic'].values contains topic.value %}


                    <h4 class="writing__category  {{topic.value}}">{{topic}}</h4>

                    {% break %}


                  {% endif %}
                {% endfor %}

                {% for post in issue.posts  %}
                  {% if post.taxonomy['Topic'].values contains topic.value %}

                        <h1 class="writing__alpha"><a href="{{post.url}}">{{post.title}}</a></h1>
                        <p class="writing__date">{{post.date | date: "%b %d, %Y"}}</p>

                  {% endif %}
                {% endfor %}
              {% endfor %}


        </div>

    </div>

{% endfor %}


{% endcapture %}

{% include 'layouts/default' %}